import { WrapInHtmlInterceptor } from './wrap-in-html.interceptor';

describe('WrapInHtmlInterceptor', () => {
  it('should be defined', () => {
    expect(new WrapInHtmlInterceptor()).toBeDefined();
  });
});
