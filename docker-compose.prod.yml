version: '3.8'
services:
  api:
    ports:
      - "80:8080"
    cpu_count: 1
    restart: unless-stopped
    healthcheck:
      test: curl -f http://localhost:8080/api/ || exit 1
      interval: 5m
      timeout: 10s
      retries: 3
      start_period: 30s
